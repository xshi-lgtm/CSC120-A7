1. In Cafe Class, we changed the order of "super(name, address, nFloors);" in the constructor - putting in the first line.
2. In Cafe Class, we modified the restock method by adding a plus mark in front of the equal mark when defining the new amount of coffee ounces : "this.nCoffeeOunces += nCoffeeOunces;"
3. In Cafe Class, we modified the sellCoffee method by adding an else condition after if condition.
4. In House Class, we modified the constructor and deleted the required inputting parameter array list of students. Instead, we created a new array list everytime we construct a new instance. This satisifies the requirement in the readme file.
5. In House Class, we missed some of the exception decription in the Java doc. We added them in this version.
6. In the House class constructor, we modified the order of the lines to let super() be in the first row.